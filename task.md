# 銘板照合PoC 整理依頼（試験バージョン確定：README一本化・バージョン確定）

既存の `PlateChecker` プロジェクト（GitHub管理下）を、**動作部分の試験バージョン完成**として整理します。工場での実機確認で動作確認済みのため、**コードの機能は変更せず、ドキュメント整理とバージョン確定のみ**を行ってください。実装後 `npm run build`（型チェック）とHTTPS開発サーバー起動を確認し、コミットしてください（push・タグpushはユーザー実行）。

## 大前提（厳守）

- **コードの機能・挙動は一切変更しない。** 工場で動作確認済みの状態を維持する。今回はドキュメントとバージョン表記のみ。
- バグ修正・リファクタリング・不要コード削除なども**今回は行わない**（安全側。動作している状態に手を入れない）。

## 1. README全体を最新仕様で一本化（書き直し）

これまで機能追加のたびに追記を重ねてきたREADMEを、**現状の最終仕様に合わせて一貫した文書に再構成**してください。追記の継ぎ接ぎ・重複・古い記述を解消し、今の実態に合った内容に書き直す。以下を網羅すること（現状実装に即して）：

- **アプリ概要**：出荷前銘板照合システム。カメラ映像で銘板をリアルタイム検査し、回答データと照合してPASS/NGを自動判定する試験バージョン。
- **主な機能**（現状の全機能を反映）：
  - 映像リアルタイム検査（30FPS表示／2FPS検査の負荷分離）
  - ガイド枠（赤）と検出枠のpx指定（5px刻み・最小100×50px・最大は映像枠実寸追従）、OpenCV輪郭検出とフォールバック
  - 行検出（自動＝水平投影／固定＝行数セレクト指定）
  - 順不同の行消し込み（一致した行から確定、全行確定で1枚OK）
  - 複数銘板の連続検査・残数カウントダウン
  - 製番→種類の2段階選択（`products[].types[].nameplates[]` 構造）
  - 前処理パイプライン（設定フォームで全項目を実適用、デフォルトthreshold有効、デフォルトに戻すボタン、設定値保持）
  - 設定メニュー（歯車・オーバーレイ・映像を見ながら調整、通常／前処理後映像の切替）
  - 映像右上のOCR生テキスト・照合ラベル表示
  - ホワイトリスト（`whitelist.jsonc`・コメント可・ビルド時import・ファイルのみ編集）
  - OCR（`jpn+eng`）／正規化（スペース除去・置換辞書）／結果JSON出力（退避）／手動検査（退避）
- **セットアップ・起動方法**：`npm install` → `npm run dev`（HTTPS開発サーバー）。
- **iPhoneでの使い方**：HTTPS必須、`npm run dev -- --host` でLAN公開、`https://<PCのIP>:5173`、自己署名証明書の許可、同一Wi-Fi・ファイアウォールの注意。
- **使い方（検査フロー）**：製番→種類を選択しセット → 検査開始（カメラ起動）→ ガイド枠調整・モード切替・行数指定 → 検査（順不同で行が消し込まれる）→ 1枚OKで残数カウント → 全数完了。
- **設定の説明**：前処理パラメータ（各項目の意味・デフォルト値・thresholdをOFFにする運用）、検出枠px、ホワイトリスト（`whitelist.jsonc` の構造・編集・再ビルド/ホットリロードで反映）。
- **データ構造**：回答データ（`products[].types[].nameplates[]`、quantity）、検査結果JSONの形式。
- **プロジェクト構成**：主要ファイル（`ocrService.ts`／`preprocessPipeline.ts`／`useVideoInspection.ts`／`vision.ts`／`whitelist.jsonc` など）の役割。
- **今後の課題・既知の制限**（下記2セクションに明記）。

文書は読みやすく構成（見出し・順序を整理）し、古い・矛盾する記述を残さないこと。

## 2. 今後の課題・既知の制限をREADMEに明記

これまで「後回し」と決めた積み残しを、「今後の課題」「既知の制限」として明記する。試験バージョンとして「どこまでが完成で、何が残っているか」を明確にする。少なくとも以下を含める：

- **検査→合格後の動作**：修正予定（精度を優先したため後回し）。
- **記号の置き換え違い**：OCRの記号取り違えを置換辞書（`REPLACEMENT_DICTIONARY`）で補正していく余地。
- **OCR精度の特性**：ホワイトリストを実績ベースで広く持つため、取りこぼしは減るが似た文字への誤認識が出る場合がある旨。前処理（threshold等）は銘板・照明により調整が要る旨。
- **外注先利用に向けて**（将来）：現状は社内LAN＋自己署名HTTPSの試験運用。社外利用時はホスティング＋アクセス制限への移行が必要。
- その他、開発過程で把握している制限があれば追記。

## 3. バージョン確定

- **`package.json` のバージョンを更新**する。試験バージョンであることが分かる版数にする（例：`0.1.0` など。具体値は妥当な初回試験版として設定し、READMEと一致させる）。
- **READMEに版数・日付・状態（動作部分の試験バージョン）を明記**する。
- **Gitタグを作成**する（例：`v0.1.0` 等、package.jsonの版数と一致）。タグのpushはユーザーが実行する前提で、**作成したタグ名と push 用コマンド（例：`git push origin v0.1.0`）を報告に明記**する。

## 制約（必ず守る）

- コードの機能・挙動は変更しない。ドキュメントとバージョン表記のみ。
- `.gitignore` の正しい状態を維持（`node_modules`・`dist` を混入させない）。
- 変更（README・package.json・タグ）は意味のある単位でコミットする。push・タグpushはユーザー実行。

## 進め方

コードには手を入れず、READMEを現状の最終仕様で一本化し、今後の課題・既知の制限を明記し、`package.json` の版数更新＋READMEへの版数記載＋Gitタグ作成でバージョンを確定してください。`npm run build`（型チェック）通過とHTTPS開発サーバー起動を確認し、コミットしてください。作成したタグ名とpush用コマンドを報告してください。
