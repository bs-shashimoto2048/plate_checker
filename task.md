# 銘板照合PoC 修正依頼（ホワイトリストを外部設定ファイル化）

既存の `PlateChecker` プロジェクト（GitHub管理下）で、現在 `ocrService.ts` 内のソース定数（`DEFAULT_OCR_OPTIONS.whitelist`）に直書きされているOCRホワイトリストを、**プロジェクトルートに新規作成する設定ファイルに切り出し、そこから読み込む**ように変更してください。実装後 `npm run build`（型チェック）とHTTPS開発サーバー起動を確認し、意味のある単位でコミットしてください（pushはユーザー実行）。

## 要件

### 1. 設定ファイルの新規作成（プロジェクトルート・JSON）

- プロジェクトの**ルート**に `whitelist.json`（JSON形式）を新規作成する。
- 現在 `ocrService.ts` に直書きされているホワイトリスト文字列を、このファイルへ移す。**現在の文字列の内容は変えずそのまま移植**すること（＝移行であって中身の変更ではない）。現在値（参考）：

```
電灯高圧低受停復電制御用検出確認盤配分岐電源点棟コンセント次動力トランスリレータイマ・ ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789.-_/()
```

- JSON構造は、編集しやすさを考慮してよい（例：1本の文字列で持つ、またはカテゴリ別の配列を結合して1つのホワイトリスト文字列にする等）。ただし**過度に複雑化せず、人が編集しやすい簡潔な構造**にする。最終的にOCRへ渡すのは結合後の1つの文字列。
- どういう構造で持っているか、編集方法をREADMEに明記する。

### 2. ビルド時importで読み込む

- `whitelist.json` を**ビルド時にimport**して読み込む（実行時fetchではない）。
- ルート直下のJSONをimportするため、Vite/TypeScriptの設定に注意：`tsconfig` の `resolveJsonModule` が有効であること、import文がルートのファイルを正しく解決できること（パス・`vite.config.ts` の必要な設定）を確認・対応する。ビルドが通ること。
- ファイルを変更したら再ビルド／開発サーバーのホットリロードで反映される、という運用でよい。
- 読み込んだ値を、現在ホワイトリストを参照している箇所（`ocrService.ts` の `DEFAULT_OCR_OPTIONS.whitelist` 等）へ供給する。**ホワイトリストの唯一の編集元を `whitelist.json` に一本化**する。

### 3. 既存挙動の維持

- ホワイトリストは引き続き、**映像検査・手動検査の両経路でOCRに効く**こと（生成直後＋認識直前の二重適用という既存の効かせ方を維持）。設定元がソース定数から `whitelist.json` に変わるだけで、効き方は変えない。
- ホワイトリストは引き続き**ファイル（設定）でのみ編集**し、画面UIからは編集しない（編集UIは作らない）。
- 万一 `whitelist.json` の読み込みに失敗した場合のフォールバック（空にしない等、安全な既定）を用意してもよいが、基本はビルド時importなので読み込めない場合はビルドエラーで検知される想定。

## 制約（必ず守る）

- ホワイトリストの**中身は変更しない**（移行のみ）。
- OCR入力の範囲・アスペクト比・前処理パイプライン・検査ロジック（30/2FPS分離・順不同消し込み・正規化/照合・行検出・`jpn+eng`）は変更しない。
- デフォルト値（前処理設定等）は今回変更しない（明示指示なき限り不変、の運用を継続）。
- 既存のレイアウト・揺れない設計・1画面の収まりに影響を与えない（UIは変えない）。

## README更新

- ホワイトリストの編集元が `whitelist.json`（プロジェクトルート）になった旨と、その構造・編集方法。
- ビルド時importで読み込むため、変更後は再ビルド／ホットリロードで反映される旨。
- 画面UIからは編集しない（ファイルのみ）旨。

## 進め方

`ocrService.ts` に直書きのホワイトリストを、ルートの `whitelist.json` へ中身を変えずに移し、ビルド時importで読み込んで唯一の編集元に一本化してください。`resolveJsonModule` 等の設定を確認し、ビルドが通ること、両経路でホワイトリストが従来どおり効くことを確認してください。実装後 `npm run build`（型チェック）通過とHTTPS開発サーバー起動を確認し、コミットしてください。実映像での確認はREADME手順どおり依頼者側で行います。
