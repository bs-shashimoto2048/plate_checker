# 銘板照合PoC 修正依頼（リポジトリ管理開始・検出枠px化・前処理設定日本語化・設定メニュー表示改善）

既存の `PlateChecker` プロジェクトに、以下4点を実装してください。ここからは影の影響など微妙な調整段階に入るため、**慎重に・既存挙動を壊さないこと**を重視します。実装後 `npm run build`（型チェック）とHTTPS開発サーバー起動を確認してください。

## 0.（作業前）リポジトリ管理を開始する

実装作業に入る前に、以下を行ってください。

- Gitリポジトリを初期化する（未初期化の場合）。
- **コードを上げる前に `.gitignore` を整備**し、不要・秘匿ファイルを適切に除外する。少なくとも `node_modules`、`dist`、ビルド成果物、ログ、`.env` 等の環境ファイル、OS生成ファイル（`.DS_Store` 等）、エディタ設定（`.vscode` 等は必要に応じて）を除外する。
- リモートに以下を設定する：`https://github.com/bs-shashimoto2048/plate_checker.git`
- 初回コミットを作成し、リモートへpushする。
- **認証（push権限）が通らない場合は、そこで作業を止めて報告**してください（無理に進めない）。`.gitignore` 整備と初回コミットまではローカルで完了させ、push可否を明示すること。

以降の各変更も、意味のある単位でコミットしてください。

## 1. 検出枠サイズの設定をpx単位に変更

現在は幅・高さが％指定だが、これをpx単位に変更する。

- 検出枠の幅・高さを**px単位**で調整できるようにする。
- **5pxステップ**で調整する。
- **最小：100px × 100px**。
- **最大：映像枠の表示実寸（px）に合わせる**。画面幅・回転・表示領域の変化で映像枠の実寸が変わったら、最大値もそれに追従して動的に決まるようにする（固定値にしない）。
- px基準は**おまかせ（推奨で実装）**。検出枠は「表示を見ながら合わせる」ものなので、画面に映っている映像枠のpx（表示px）を基準にするのが妥当。OCR切り出し用の実映像解像度との換算は内部で吸収し、ユーザーが指定したpx範囲＝映像枠上で見えている範囲となるようにすること。
- スライダーまたは数値入力で、現在のpx値が分かるように表示する（例：「幅 320px」）。
- 検出枠の中心固定での伸縮、OpenCV輪郭検出失敗時のフォールバックなど既存仕様は維持。

## 2. 前処理設定の日本語化（ラベル・選択値とも）

設定メニュー内の前処理設定フォーム（前回追加した、値の保持・表示・編集のみのフォーム）を日本語化する。

- **項目名（ラベル）を日本語化**する。ただし**冗長を避け簡潔に**し、操作UIのレイアウトを崩さない。
- **選択値（セレクトの中身）も日本語化**する（`binary` / `close` / `gaussian` 等を日本語表記にする。例は実装側で適切な訳語を当ててよい）。
- 内部で保持する値（データ構造・キー）は英語のまま（前回のJSON構造 `preprocess.operations.*` を維持）。**表示だけ日本語化**し、データの持ち方・型は変えない。次段階の処理適用に支障が出ないようにする。
- フォームのレイアウト・操作性（個別UIコントロール）は前回のまま。日本語化で項目が長くなって折り返す・はみ出す場合は、簡潔な訳語にして収める。

## 3. 設定メニューの表示位置・スクロール改善

前処理の値を変えながら映像（前処理後表示）を見たいので、設定メニューが映像に被らないようにする。

- 設定メニューを開いたとき、**表示の開始位置を下げ、映像枠に被らない**ようにする（映像枠が見えた状態で設定を操作できる）。
- **メニュー内はスクロール**で操作できるようにする（項目が多くても、映像を見ながらメニュー内をスクロールして各値を変更できる）。
- 設定メニューはオーバーレイのまま、本体レイアウトを押し広げない・揺らさない。映像枠＋設定メニューが同時に見える配置にすること。

## 制約（必ず守る）

- ここからは慎重に。**既存の検査挙動・レイアウト・揺れない設計を壊さない**。
- 検査ロジック（30/2FPS分離・順不同消し込み・OCR/正規化/照合・枚数カウント・行検出・現行のグレースケール＋軽い補正・アスペクト比保持のOCR入力）は変更しない。
- 前処理設定フォームは引き続き**値の保持・表示・編集のみ**（処理適用は次段階。今回も適用しない）。
- 1画面の収まりを維持。

## 維持する仕様（変更しない）

- アスペクト比を保持したOCR入力（前回修正）、グレースケール＋軽い補正の前処理。
- 自動/固定モード（行数セレクト）、ボタン横並び、歯車設定メニュー（オーバーレイ）、通常/前処理後映像Switch、製番→種類2段階選択、検査内容テーブル、OCR/照合ラベル表示、ホワイトリスト、`jpn+eng`、結果JSON出力（退避）、手動検査（退避）。

## README更新

- リポジトリ管理開始と `.gitignore` の方針。
- 検出枠サイズがpx単位（5pxステップ・最小100×100px・最大は映像枠実寸に追従）になった旨。
- 前処理設定フォームの日本語化（ラベル・選択値、内部キーは英語維持）。
- 設定メニューの表示位置（映像に被らない）とメニュー内スクロール。

## 進め方

まず作業前にリポジトリ管理を開始し（`.gitignore` 整備→初回コミット→push、認証不可なら停止報告）、その後で1〜3を実装してください。ここからは微調整段階のため、既存挙動を壊さないことを最優先に慎重に進めてください。実装後 `npm run build`（型チェック）通過とHTTPS開発サーバー起動を確認し、検出枠px化（5px刻み・最小100px・最大が映像枠実寸に追従）、前処理設定の日本語化、設定メニューが映像に被らずスクロール可能、が動作することを確認してください。実映像での見え方の最終確認はREADME手順どおり依頼者側で行います。